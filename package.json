{
  "name": "@garage/design-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "pnpm --filter @garage/web-shell typecheck",
    "test": "pnpm --filter @garage/web-shell test",
    "check": "pnpm typecheck && pnpm test"
  }
}
